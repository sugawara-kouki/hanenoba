<%- if controller_name != 'sessions' %>
  <p><%= link_to "ログイン", new_session_path(resource_name), class: "text-blue-600 hover:text-blue-500 text-sm font-medium" %></p>
<% end %>

<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  <p><%= link_to "新規登録", new_registration_path(resource_name), class: "text-blue-600 hover:text-blue-500 text-sm font-medium" %></p>
<% end %>

<%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
  <p><%= link_to "パスワードをお忘れですか？", new_password_path(resource_name), class: "text-blue-600 hover:text-blue-500 text-sm font-medium" %></p>
<% end %>

<%- if devise_mapping.confirmable? && controller_name != 'confirmations' %>
  <p><%= link_to "確認メールが届きませんか？", new_confirmation_path(resource_name), class: "text-blue-600 hover:text-blue-500 text-sm font-medium" %></p>
<% end %>

<%- if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks' %>
  <p><%= link_to "アカウント凍結解除メールが届きませんか？", new_unlock_path(resource_name), class: "text-blue-600 hover:text-blue-500 text-sm font-medium" %></p>
<% end %>

<%- if devise_mapping.omniauthable? %>
  <%- resource_class.omniauth_providers.each do |provider| %>
    <p><%= button_to "#{OmniAuth::Utils.camelize(provider)}でログイン", omniauth_authorize_path(resource_name, provider), data: { turbo: false }, class: "text-blue-600 hover:text-blue-500 text-sm font-medium pointer-cursor" %></p>
  <% end %>
<% end %>

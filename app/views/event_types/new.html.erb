<% content_for :title, "New event type" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New event type</h1>

  <%= render "form", event_type: @event_type %>

  <%= link_to "Back to event types", event_types_path, class: "w-full sm:w-auto text-center mt-2 sm:mt-0 sm:ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-200 text-gray-700 inline-block font-medium" %>
</div>
